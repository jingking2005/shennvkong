{
  "name": "summon-hall",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 3100 --strictPort",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run && python3 scripts/smoke_test.py",
    "test:unit": "vitest run",
    "data:validate": "node scripts/validate-runtime-data.mjs",
    "data:build": "node scripts/build-archive-manifests.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.9"
  }
}
