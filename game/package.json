{
  "name": "valkyrie-crusade-game",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "data:validate": "tsx src/data/pipeline/validate.ts",
    "data:normalize": "tsx src/data/pipeline/normalize.ts",
    "data:report": "tsx src/data/pipeline/report.ts"
  },
  "dependencies": {
    "phaser": "^3.80.1"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^5.4.0",
    "vitest": "^2.0.0",
    "tsx": "^4.16.0",
    "eslint": "^9.0.0"
  }
}
